{
  "name": "jss-clips-front-door-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "config": "node ../scripts/write-worker-config.js clips-worker",
    "check": "npm run build:web && npm run config && wrangler types && tsc --noEmit && wrangler deploy --dry-run",
    "deploy": "npm run build:web && npm run config && wrangler deploy --minify",
    "types": "npm run config && wrangler types"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1107.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.114.0"
  }
}
